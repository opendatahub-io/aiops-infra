name: Skills Tests

on:
  push:
    branches: [main]
    paths: ["skills/**/scripts/**", "tests/unit/**", "tests/conftest.py", "pyproject.toml"]
  pull_request:
    branches: [main]
    paths: ["skills/**/scripts/**", "tests/unit/**", "tests/conftest.py", "pyproject.toml"]

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -e ".[dev]"
      - run: pytest tests/unit/ -k "conforma" --cov --cov-report=term-missing
