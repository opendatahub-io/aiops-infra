# skillsaw configuration
# https://github.com/stbenjam/skillsaw

version: "0.10.1"

rules:
  context-budget:
    enabled: true
    severity: warning
    limits:
      skill:
        warn: 7000
        error: 10000

  content-critical-position:
    enabled: true
    severity: info

strict: true
